# Core dependencies - CPU only (PyTorch installed separately in Dockerfile)
transformers==4.46.3
safetensors==0.5.3
huggingface-hub==0.33.4

# FastAPI and server
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20

# Audio processing - CPU only versions
librosa==0.11.0
soundfile==0.12.1

# Utilities
numpy==1.26.4
pyyaml==6.0.2
tqdm==4.67.1
requests==2.32.4
scipy==1.11.4

# Chatterbox dependencies
einops==0.8.1

# Explicitly avoid CUDA packages
--extra-index-url https://download.pytorch.org/whl/cpu
# Remove all nvidia-* packages - not needed for CPU
# Remove triton - GPU dependency
